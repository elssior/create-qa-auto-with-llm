# Базовые зависимости
qwen-agent[gui,code_interpreter]

# Для работы с Ollama (локальная модель)
ollama

# Для парсинга конфигурации
pyyaml

# Для тестирования сгенерированных автотестов (опционально, но рекомендуется)
pytest

# (Опционально) Если вы будете использовать RAG для анализа кода — раскомментируйте:
# qwen-agent[rag]